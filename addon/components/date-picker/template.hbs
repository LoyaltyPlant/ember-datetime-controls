{{date-control
    date=date
    minDate=minDate
    maxDate=maxDate
    disabledDates=disabledDates
    locale=locale
    timeZone=timeZone
    format=format
    onChange=onChange
    size=size
    allowClear=allowClear
    isDisabled=isDisabled
    placeholder=placeholder
}}
