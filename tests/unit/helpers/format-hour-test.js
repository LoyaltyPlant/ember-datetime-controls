import { formatHour } from 'dummy/helpers/format-hour';
import { module, test } from 'qunit';

module('Unit | Helper | format hour');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatHour([42]);
  assert.ok(result);
});

